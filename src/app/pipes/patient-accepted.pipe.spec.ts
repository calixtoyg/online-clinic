import { PatientAcceptedPipe } from './patient-accepted.pipe';

describe('PatientAcceptedPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientAcceptedPipe();
    expect(pipe).toBeTruthy();
  });
});
