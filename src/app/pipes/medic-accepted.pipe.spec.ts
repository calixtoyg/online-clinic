import { MedicAcceptedPipe } from './medic-accepted.pipe';

describe('MedicAcceptedPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicAcceptedPipe();
    expect(pipe).toBeTruthy();
  });
});
