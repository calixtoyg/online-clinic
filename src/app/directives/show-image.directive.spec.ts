import { ShowImageDirective } from './show-image.directive';

describe('ShowImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowImageDirective();
    expect(directive).toBeTruthy();
  });
});
